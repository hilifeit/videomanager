import 'package:videomanager/screens/others/exporter.dart';

class TypingWidget extends StatelessWidget {
  const TypingWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
